# T6 — Comment diet, local-llm and devbox

`invariant: drvPath` — comments only. All 15 outputs byte-identical.

## Read this before anything else: the ledger is stale for local-llm
TW walked the repo at `ff825ef`. **PRs #165 and #166 landed after that**,
re-enabling MTP on a SHA-pinned nightly and fitting its config to the card.
They rewrote large parts of `modules/local-llm/{models,nixos,vllm-service}.nix`
and added most of the comments now in them.

`models.nix` went from 103/261 (39%) to **226/385 (58%)** comment lines. That
growth is *new rationale written hours ago*, not accumulated cruft.

So: **LEDGER.md's per-comment verdicts for local-llm describe text that in many
cases no longer exists.** Use the ledger for `devbox/*`, where it is current.
For `local-llm/*`, re-derive from the file, and treat anything that cites
`docs/ninfer-vs-vllm-2026-09-03/` or discusses MTP, mamba cache poisoning, lane
capping or memory fit as **fresh and load-bearing until proven otherwise**.

## Targets
| file | comments/total |
|---|---|
| `modules/local-llm/models.nix` | 226/385 = 58% |
| `modules/local-llm/nixos.nix` | 124/298 = 41% |
| `modules/devbox/container.nix` | 141/345 = 40% |
| `modules/local-llm/vllm-service.nix` | 67/176 = 38% |
| `modules/devbox/nixos.nix` | 35/271 = 12% |
| `modules/local-llm/container.nix` | 8/48 = 16% |
| `modules/devbox/agents.nix` | 8/31 = 25% |
| `modules/local-llm/weights.nix` | 2/13 = 15% |

The last four are already lean. Confirm and move on; do not manufacture cuts.

## The standard, and the specific risk here
Rubric A, default delete. **But this directory is the repo's densest
concentration of operational knowledge that cost real debugging to acquire** —
vLLM version-specific workarounds, upstream issue numbers, MTP/prefix-cache
corruption, KV behaviour, vision propagation, tuning arithmetic. PLAN.md flags
exactly this: "several comments here encode live operational facts. Those get
relocated, not deleted."

For every cluster, decide KEEP-IN-PLACE / RELOCATE / CUT and say which. Bias to
keep. A tuning number with its evidence is not "design prose" — it is the
reason the number is what it is, and rubric A's first keep-test protects it.

**Relocation target:** `docs/ninfer-vs-vllm-2026-09-03/` already exists and is
current — it is #165's research. Prefer adding to it, or a new dated
`docs/local-llm.md`, over inventing a scheme. Whatever you pick: title, dated
status line (D2), pointer from the module, link back. Never `docs/repo-hygiene/`.

## devbox/container.nix
Here the ledger is current. It is the repo's largest module file and TW judged
it **overloaded** — but that is T7's problem, not yours. Diet its comments only;
do not move code. TW's comment verdicts: mostly KEEP-IN-PLACE (vendor-quirk and
load-bearing clusters at 18-28, 199-214, 276-293), CUT 222-230 (redundant with
64-75/199-214) and 312-313 (duplicates its own header and nixos.nix's).

## Acceptance
1. drvPath loop byte-identical to `docs/repo-hygiene/DRVPATH-BASELINE.txt`
   (`diff <(loop) <(grep -v '^# ' docs/repo-hygiene/DRVPATH-BASELINE.txt)`).
2. `nix flake check` green.
3. Before/after comment counts per file, with **why the survivors survive**.
4. An explicit list of anything you cut from `local-llm/*` that was added by
   #165/#166, with your justification. Cutting hours-old rationale is the single
   most likely mistake in this task; make it easy to audit.
5. Every RELOCATE lands somewhere real, linked both ways.
6. Anything you were unsure about, listed.
7. `git diff --stat` shows only these modules and any docs file you added.
