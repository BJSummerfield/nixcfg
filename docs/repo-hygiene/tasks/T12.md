# T12 — Final sweep

`invariant: drvPath` — a survey, a README touch-up and a directory deletion
cannot change a build. All 15 outputs byte-identical.

## 1. Re-run the survey, and be honest about the numbers
FINDINGS.md's baseline was `129 .nix, 9291 lines, 1246 comment lines (13.4%)` at
`1b12ba9`. **Do not compare against that** — main gained PRs #165 and #166
mid-effort, which added substantial local-llm config and rationale. Comparing to
the old baseline would credit this effort with someone else's additions or blame
it for them.

The honest comparison is `origin/main` vs `hygiene`, measured today:

| | nix files | nix lines | comment lines | ratio |
|---|---|---|---|---|
| main | 132 | 9510 | 1429 | 15% |
| hygiene | 135 | 9366 | 1154 | **12%** |

Three more files (T7's splits and the address registry) and fewer lines, which
is the shape you want from a restructuring: more files, each doing less.

State it that way in the PR body. If your own measurement disagrees with the
above, trust your measurement and say so.

## 2. README accuracy pass
The README was written at T2 and five tasks have landed since. Check every
claim still holds, in particular:
- The option namespace section: `mine.accounts.*` now exists (T4 renamed it).
  T4 was supposed to delete the parenthetical hedge — confirm it is gone.
- Anything describing module layout: T7 added `modules/devbox/home.nix`,
  `modules/users/home-manager.nix`, `modules/containers/addresses.nix`.
- The checks paragraph, if the check set changed (it did not, but verify).
- Any command: re-trace each one. T5/T6/T8 moved things.

**Add nothing that will rot.** No counts, no file lists that must be maintained.

## 3. Delete `docs/repo-hygiene/`
Rubric D5: this directory is an artifact like any other, and D1 applies to it.
It goes. Git history is the archive, and the PR bodies carry the reasoning.

**Before deleting, check nothing outside it references it.** `grep -rn
'repo-hygiene'` across the repo — README, docs/, modules, CI, tests. Any hit
outside the directory is a finding: fix the reference, then delete.

**Two things in there are referenced by live code and must survive somewhere or
be confirmed unnecessary:**
- `DRVPATH-BASELINE.txt` — the invariant baseline. After T12 there are no more
  tasks, so nothing needs it. Confirm no code or CI reads it, then let it go.
- `LEDGER.md` — 2249 lines of per-file survey. Nothing references it. It goes
  with the rest; `git log` has it.

## 4. Confirm the gate still gates
The whole effort rests on T1's checks. Plant one violation of each
(unformatted file, statix, deadnix), confirm `nix flake check` fails on each,
revert. This is the last chance to notice that the enforcement quietly stopped
working sometime in the last twelve tasks.

## Acceptance
1. drvPath loop byte-identical to `DRVPATH-BASELINE.txt` — run this **before**
   deleting the directory, obviously.
2. `nix flake check` green after the deletion.
3. `grep -rn 'repo-hygiene'` returns nothing.
4. The three gate-plant results, with the failure messages.
5. Your measured main-vs-hygiene table.
6. Every README claim you re-verified, and anything you corrected.
